export function OrderFilter (filteredArray, setFilteredArray, event) {
  if (event.target.name === 'Precio') {
    console.log(event)
  }
  if (event.target.name === 'Fecha') {
    console.log(event)
  }
}
